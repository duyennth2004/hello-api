pipeline {
  agent any
  stages {
    stage('run collection') {
      steps {
        sh 'run -t postman/newman run -h'
        sh 'run -v ${WORKSPACE}:/etc/newman --workdir /etc/newman -t postman/newman run Trello.json --color off --disable-unicode'
      }
    }
  }
}